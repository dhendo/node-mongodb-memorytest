# node-mongodb-memorytest


Test the memory usage of the node-mongodb-native v2 driver when repeatedly switching DBs

Usage:

make v1 - run 50k switches against v1.4.37

make v2 - run 50k switches against v2.0.39
